import 'package:flutter/material.dart';

class DescriptionOfExpense extends StatelessWidget {
  static const routeName='/des';
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(10),
      child: Text('f'),
    );
  }
}